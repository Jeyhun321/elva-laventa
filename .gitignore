node_modules
dist
dist-ssr
*.local

# Şəxsi lokal Claude Code ayarları
.claude/settings.local.json

# Локальный доступ к логам — service_role ключ, НИКОГДА не коммитить
tools/.env.logs
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json

# Playwright MCP local artifacts (snapshots, console logs) — never commit
.playwright-mcp/
