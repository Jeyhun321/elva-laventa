# Дневник — 2026-08-01

Каждое завершённое действие дописывается сюда с временем.
В конце дня сворачивается в `HISTORY.md` кратко, файл остаётся как есть.

---

- **[00:29]** Создан `HANDOFF.md` — документ передачи работы между Claude Code и Codex. Коммит `0d82dc9`.
- **[00:30]** Сверил состояние проекта перед заполнением. Обнаружено: Codex уже начал шаг 2 — коммит `d063dc9` переключил `accountId` на `isSignedIn`, плюс есть незакоммиченная работа по привязке кеша к владельцу. Свою работу с этим не смешивал.
- **[00:32]** Проверены незакрытые места шага 2: `orders.js:21` пропускает только Google (email-покупатель не оформит заказ), `CheckoutPage.jsx` — 3 места на `isGoogleUser`.
- **[00:33]** Запрошены настройки Supabase: `mailer_autoconfirm: false` — подтверждение email всё ещё включено.
- **[00:35]** Настроена трёхуровневая система файлов: `HANDOFF.md` (текущий момент, перезаписывается), `HANDOFF.daily-ГГГГ-ММ-ДД.md` (этот файл), `HISTORY.md` (вся история). В `HANDOFF.md` добавлено правило чтения: при старте сессии открывать только его. Коммит `269df78`.
- **[01:10]** `src/lib/orders.js`: снята блокировка заказа для не-Google. Теперь достаточно быть вошедшим — `place_order` и так пишет `auth.uid()`. Код ошибки `GOOGLE_AUTH_REQUIRED` → `AUTH_REQUIRED` (карта в `CheckoutPage` понимает оба).
- **[01:12]** `src/pages/CheckoutPage.jsx`: 3 места `isGoogleUser` → `isSignedIn` (48, 126, 287). Email-покупатель больше не упирается в заглушку «войдите».
- **[01:13]** Текст `order_auth_required` очищен от упоминания Google на 3 языках.
- **[01:15]** Проверено: завязок на Google в `src/` не осталось (кроме `AuthContext`). Гость по-прежнему не проходит, страницы открываются, карта ошибок верна на 3 случаях, консоль чистая, сборка чистая. Проверено на 375px и 1280px. Коммит `535338b` — **шаг 2 по коду закончен**.
- **[01:16]** Обновлены `HANDOFF.md` и этот дневник.
- **[01:18]** Codex прочитал handoff, дневник, `git log --oneline -5` и `git status`; зафиксировал текущую точку: шаг 2 завершён, две незакоммиченные правки ожидают живой проверки на реальных аккаунтах и экранах 375px/1280px. `HANDOFF.md` обновлён.
- **[01:18]** По указанию владельца Codex остановил работу с исходным кодом: пока активна работа с Claude Code, ведёт только handoff-файлы.
- **[01:45]** Возобновлена работа по новому поручению. Задача 1 уже была исправлена и проверена; для задачи 2 очистка поиска теперь сбрасывает URL-фильтр; для задачи 3 добавлен мобильный верхний отступ кнопке возврата. Задачи 4–5 оставлены без изменений до согласования плана.
- **[01:45]** Сборка после задач 2–3 успешна. Найдены причины задач 4–5: частичный `includes` для кода в `CatalogPage`, а также первичный рендер `catalog.json` до ответа Supabase в `CatalogContext`. Локальный browser-check не запустился из-за завершившегося Vite-сервера; deploy не делался.
- **[01:20]** Сессия Claude Code завершена — владелец переходит в новую. Исходники: незакоммиченными остаются только правки Codex (`ShopContext.jsx`, `ProductPage.jsx`), они ждут живой проверки. Всё остальное запушено.
- **[02:05]** Найдена НАСТОЯЩАЯ причина отказа email-покупателю: фронт (`orders.js`, `CheckoutPage.jsx`) починен ещё в `535338b`, но SQL-функция `place_order` в базе по-прежнему требует `provider = 'google'` и кидает `GOOGLE_AUTH_REQUIRED`. Проверка есть во всех трёх версиях функции (`fix-telegram-order-notify.sql:42`, `fix-order-checkout.sql:38`, `marketplace-foundation.sql:100`). RLS-политики на Google не завязаны — только эта функция.
- **[02:10]** Создан `supabase/fix-order-any-auth.sql` — копия самой свежей версии (`fix-telegram-order-notify.sql`, с Telegram-уведомлением) с единственным изменением: убран блок проверки Google. `auth.uid() is null → AUTH_REQUIRED` остался. Серверная цена, проверка размеров, `order_items`, Telegram и очистка корзины не тронуты. Применяет владелец вручную в SQL Editor.
- **[02:15]** Проверка п.4 (не потерялись ли корзины/избранное после смены `accountId`): чтением из базы через service_role. `customer_cart_items` — 2 строки / 1 пользователь, `customer_favorites` — 4 строки / 3 пользователя, `orders` — 8 заказов / 3 пользователя. Данные на месте, синхронизация работала. Для Google-аккаунтов `accountId` = тот же `user.id`, ключ localStorage не менялся → потери нет, база остаётся источником истины. Остаточный риск: если у кого-то корзина жила ТОЛЬКО в браузере и не доехала в базу — Codex убрал автоперенос (`mustPush`), такая корзина пропадёт молча. По факту базы таких случаев не видно.
- **[02:20]** Toggle размера проверен вживую на dev-сервере: 375px — выбор → снятие → выбор → переключение на другой размер; 1280px — то же самое плюс XL. Ошибок в консоли и в логах Vite нет. Скриншот снять не удалось (панель браузера не отображалась) — проверка сделана по классу `active` после реальных кликов.
- **[01:45]** После «ок» реализованы задачи 4–5: код товара переведён на полное совпадение; при Supabase локальный каталог больше не попадает в первый render, вместо него показывается loading-state, а `catalog.json` остаётся fallback после двух неудачных запросов.
- **[01:45]** Финальная проверка на локальном Vite: 375px — loading без старых карточек, затем 14 свежих; очистка поиска возвращает все 14; back-link имеет 12px сверху. 375px/1280px — `2` даёт 0 совпадений по коду, `2002` даёт 1. Desktop toggle M снимается повторным кликом. Ошибок браузера нет; build и diff-check успешны. Тестовый сервер остановлен, deploy не выполнялся.
- **[02:06]** Повторно проверен `fix-order-any-auth.sql`: относительно Telegram-версии удалён только Google-provider блок; auth.uid, цена, размеры, Telegram, очистка корзины и права сохранены. Применить production SQL нельзя: CLI/MCP/access token/project ref/DB password отсутствуют. Read-only production Auth settings: `mailer_autoconfirm=false`, Google включён, signup разрешён. Значит владельцу вручную применить SQL и выключить Confirm email в Dashboard. Исходный код не менялся.
- **[02:06]** Финальный production build (`npm run build`) успешен. Далее: повторная UI-проверка 375px/1280px и публикация через push в `main`.
- **[02:06]** UI-проверка перед deploy успешна: 375px — 14 карточек и 12px верхний отступ back-link; 1280px — поиск `2` = 0, `2002` = 1; console errors = 0. Готово к push в `main`.
- **[02:06]** Создан commit `cecbdb2` для публикации актуальной версии; далее push в `main` запускает GitHub Pages.
- **[02:06]** Commit был дополнен handoff-статусом и отправлен как `c040ad4`; GitHub Pages workflow `30672171846` для этого SHA завершился `success`. Локальный рабочий каталог перед обновлением handoff был чистым.
- **[02:06]** Начата точечная mobile-правка RU/AZ/EN карточек: удалён счётчик результатов каталога; ≤640px рейтинг отзывов сделан nowrap с компактными метриками, название ограничено двумя строками с одинаковой высотой. Далее — проверка 375px/desktop и deploy.
- **[02:06]** Проверено: RU/AZ/EN на 375px — переполнения рейтинга нет, RU `0 отзывов` nowrap, нижние части соседних карточек выровнены; на desktop 14 карточек, счётчика результатов нет, console errors 0. Далее build + deploy.
- **[02:06]** Build успешен, mobile-правка отправлена как `00a63b9`; GitHub Pages workflow `30672984445` завершился success. Production mobile (375px): 14 карточек, счётчика нет, RU reviews nowrap без overflow, console errors 0.
- **[03:33]** Начата новая точечная задача. Причина возврата favorites: `toggleFavorite` сразу сохранял локальный cache/state, а Supabase INSERT/DELETE запускал через `void` без ожидания результата. Исправлено: state/cache меняются только после успешной записи; async-загрузка и mutation дополнительно сверяются с актуальным `user.id`. Добавлен mobile-only marquee для пустого unfocused search placeholder; счётчик товаров уже отсутствует в исходнике. Далее build, UI и deploy.
- **[03:33]** Build после изменений успешен. При фокусе search placeholder теперь остаётся статичным (анимация выключена), при вводе скрывается.
- **[03:33]** На первой 375px-проверке найдено и исправлено CSS-наследование: mobile placeholder-layer получал базовый `display:none`; теперь явно отображается только на ≤900px. Далее повторная UI-проверка.
- **[03:33]** Повторная 375px-проверка выявила `position:static` у mobile search: overlay позиционировался относительно header. Исправлено на `position:relative`, чтобы анимация ограничивалась самим полем.
- **[03:33]** Локальная UI-проверка: 375px AZ/RU/EN — счётчик отсутствует, placeholder внутри input, при focus статичен, при вводе скрыт; RU rating не переполняется. 1280px — placeholder overlay выключен, 14 карточек, счётчика нет. Console errors: 0. Далее финальный build и deploy.
- **[03:33]** Финальный `npm run build` и `git diff --check`: OK. Далее commit/push и production-check.
- **[03:33]** Создан commit `b727ddf` (`Fix mobile search and favorite sync`); далее push в `main`, GitHub Pages и production-check.
- **[03:33]** Commit дополнен handoff и отправлен как `e8fdcba`; GitHub Pages workflow `30673540932` завершился success. Production 375px: 14 карточек, счётчик отсутствует, placeholder анимируется внутри input, console errors 0.
- **[03:45]** Получена финальная задача: production favorites bug остаётся; отдельно нужен менее массивный mobile-бейдж «Новинка». Начата точечная диагностика цепочки favorites, затем будут build/UI/deploy.
- **[04:05]** Найдена remaining race condition favorites: проверка только `user.id` не отличала старый сеанс A от нового после A→B→A. Добавлен session-token на каждую активацию аккаунта; stale SELECT/DELETE/UPSERT больше не применяются к state/cache. На ≤640px бейдж «Новинка» уменьшен до компактной полупрозрачной capsule-label. Далее build и UI/deploy.
- **[04:05]** Build и diff-check: OK. Локально: 375px RU «Новинка» 64×18px внутри фото; AZ/EN без переполнения; 1280px сохраняет исходный desktop-бейдж; console errors 0. Далее deploy и production-check.
- **[04:05]** Commit `11a751e` отправлен в `main`; GitHub Pages workflow `30674785290` success. Production 375px: RU-бейдж 64×18px, внутри фото, console errors 0. Живая A/B favorites-проверка не выполнена: в браузере нет авторизованных пользовательских сессий, создание/переключение аккаунтов без владельца не производилось.
- **[04:25]** По новому видео проверена вся цепочка favorites: второго provider/effect/realtime/listener нет. Найдено смешение cache/remote initial state и closure-derived `setFavorites(next)` после DELETE. Favorites-cache отключён для вошедших; после успешного DELETE/UPSERT UI получает только fresh SELECT текущего `user.id`, с session-token guard. Далее build и воспроизведение.
- **[04:25]** Build/diff-check OK. Удалён больше не используемый writer favorites-cache: localStorage теперь не может быть источником или целью записи favorites для вошедшего пользователя. В `ShopContext` осталось только три setFavorites: очистка, initial fresh SELECT и fresh SELECT после mutation.
- **[04:25]** Commit `6c63dd4` (`Use fresh favorites after mutations`) отправлен в main; GitHub Pages workflow `30675269482` success. Production ожидает живого воспроизведения из видео: delete → ожидание → refresh на авторизованном аккаунте.
- **[04:40]** После сообщения владельца про «память каждого аккаунта» найдена гонка одного accountId: старый SELECT мог прийти после более нового DELETE и пересобрать favorites прежним snapshot. Добавлен monotonic request-version: примениться может только самый свежий SELECT/mutation текущего session-token. Память аккаунта остаётся в Supabase по user.id; localStorage не используется во избежание переноса.
- **[04:40]** Commit `ff8037f` (`Prevent stale favorite request overwrites`) отправлен в main; GitHub Pages workflow `30675626733` success. Production smoke-check на 375px/1280px: favorites route открыт, console errors 0. Для финального подтверждения нужен авторизованный A: delete → ожидание → refresh, затем A→B→A.
- **[04:55]** Получен follow-up: orphan ID давал badge «1» при пустом избранном, а cart имеет ту же stale-request проблему. Начата точечная правка: badge считает только существующие товары, cart mutation → fresh SELECT с request/session guards.
- **[04:55]** Self-debug, build и diff-check OK. В `ShopContext` добавлены отдельные cart request-version/refresh после mutation; fav/cart badge считают только товары, реально доступные в catalog. Далее deploy.
- **[04:56]** Исправлен асинхронный caller cart: ProductPage показывает «добавлено» или переходит в cart только после успешной записи/свежего SELECT, а не по truthy Promise.
- **[04:56]** `git diff --check` OK. Первая build в sandbox остановлена ограничением Windows (`esbuild` не может прочитать `vite.config.js`); запускается та же build вне sandbox.
- **[04:57]** Production build вне sandbox: OK (118 modules transformed). Далее commit/push и production-check.
- **[04:58]** Commit `d68681a` (`Fix cart sync and orphan badges`) отправлен в `main`; ожидается GitHub Pages deploy и production-check.
- **[04:59]** GitHub Pages workflow `30676069231` для `d68681a`: success. Production smoke-check 375px/1280px: badges 0 для гостевой сессии, console errors 0. Живая A/B проверка требует авторизованных аккаунтов владельца.
- **[05:00]** Начата точечная правка: account switch → home, logo → hard reload home, mobile search placeholder немного меньше/легче; home не блокируется spinner при медленной загрузке каталога.
- **[05:01]** Production build и diff-check: OK. Redirect учитывает только смену account id, обычный refresh/deep link не затрагивается. Далее deploy.
