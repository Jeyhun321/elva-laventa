# HANDOFF — где я сейчас

Обновлён: **2026-08-01 00:35**

> **Правило чтения (экономия лимита).** При старте сессии читать **только этот файл** + `git log -3` + `git status`.
> `HANDOFF.daily-*.md` и `HISTORY.md` **не открывать**, пока владелец прямо не попросит.

---

## На чём остановились

Регистрация и вход по **email + пароль** в дополнение к Google. Делаем в 2 шага.

- **Шаг 1 — готов** (`24721be`): вход/регистрация по email, ошибки на 3 языках, ссылка «Войти по почте» в окне входа. Привязка корзины намеренно не тронута.
- **Шаг 2 — в работе.** Ядро сделал Codex (`d063dc9`): `accountId = isSignedIn ? user?.id : null` — корзина теперь у любого вошедшего.

⚠️ **Незакоммичено — работа Codex, не трогать:** `src/context/ShopContext.jsx` (~71 строка) — привязка кеша к владельцу (`{accountId, items}` в localStorage вместо голого массива, чтобы данные не утекали между аккаунтами на устройстве) и `src/pages/ProductPage.jsx` (2 строки).

---

## Что осталось сейчас

1. **`src/lib/orders.js:21`** — `GOOGLE_AUTH_REQUIRED`, заказ пропускает только Google. **Email-покупатель не оформит заказ.** Самое срочное.
2. **`src/pages/CheckoutPage.jsx`** — 3 места на `isGoogleUser` (48, 126, 287): email-покупателю покажется заглушка «войдите».
3. **Подтверждение email в Supabase всё ещё ВКЛЮЧЕНО** (`mailer_autoconfirm: false`). Владелец не нашёл тумблер «Confirm email». Пока включено — после регистрации ждут письмо, а встроенная почта Supabase шлёт пару писем в час.

**Отложено (планы есть, кода нет):** избранное на уровне группы цветов · этап 4 вариантов (цвет в `order_items` и в Telegram).

---

## Что проверить дальше

1. Email-покупатель: регистрация → вход → корзина → **оформление заказа** (сейчас упрётся в `orders.js`).
2. Google-аккаунт: корзина и избранное **не потерялись** после переключения `accountId`.
3. Два аккаунта на одном устройстве — корзины не смешиваются.
4. Всё проверять **и на 375px, и на 1280px**.

---

## Последний коммит

```
0d82dc9  HANDOFF.md — Claude Code ve Codex arasinda otürme sənədi
d063dc9  Allow authenticated email users to use cart      ← последний рабочий
```

---

## Нельзя сломать

- **Проверка входа — в одной точке:** начало `addToCart` и `toggleFavorite` в `ShopContext`. Вызывающая сторона обязана уважать возвращаемое `false`.
- **Гостевая корзина отключена намеренно** — решение переигрывалось дважды, не менять без владельца.
- **`place_order`** берёт цену на сервере. Не трогать.
- **Варианты цвета:** галерея по **папке фото**, не по коду (коды у цветов общие).
- Фото товаров на localhost дают 404 — это норма, смотреть на живом сайте.
