# HANDOFF — где я сейчас

Обновлён: **2026-08-01 01:16**

> **Правило чтения (экономия лимита).** При старте сессии читать **только этот файл** + `git log --oneline -5` + `git status`.
> `HANDOFF.daily-*.md` и `HISTORY.md` **не открывать**, пока владелец прямо не попросит.

---

## На чём остановились

Регистрация и вход по **email + пароль** в дополнение к Google.

- **Шаг 1 — готов** (`24721be`): вход/регистрация по email, ошибки на 3 языках, ссылка «Войти по почте».
- **Шаг 2 — код готов.** Ядро сделал Codex (`d063dc9`): `accountId = isSignedIn ? user?.id : null`. Я закрыл остаток (`535338b`): `orders.js` и `CheckoutPage` больше не требуют именно Google.

Завязок на Google в `src/` не осталось — только в `AuthContext`, где `isGoogleUser` живёт как отдельный признак.

⚠️ **Незакоммичено — работа Codex, не трогать:** `src/context/ShopContext.jsx` (привязка кеша к владельцу: `{accountId, items}` в localStorage, чтобы данные не утекали между аккаунтами) и `src/pages/ProductPage.jsx` (2 строки).

---

## Что осталось сейчас

1. **Живая проверка на настоящих аккаунтах** — кода больше не нужно, нужен владелец. Пункты ниже.
2. **Подтверждение email в Supabase ВКЛЮЧЕНО** (`mailer_autoconfirm: false`). Владелец не нашёл тумблер «Confirm email». Пока включено — после регистрации ждут письмо, а встроенная почта Supabase шлёт пару писем в час.
3. **Codex доделывает** изоляцию кеша между аккаунтами (см. незакоммиченное).

**Отложено (планы есть, кода нет):** избранное на уровне группы цветов · этап 4 вариантов (цвет в `order_items` и в Telegram).

---

## Что проверить дальше

1. **Email-покупатель: полный путь** — регистрация → вход → корзина → **оформление заказа**. Раньше упирался в `orders.js`, теперь должен пройти.
2. **Google-аккаунт: корзина и избранное не потерялись** после переключения `accountId`.
3. **Два аккаунта на одном устройстве** — корзины не смешиваются.
4. Всё **и на 375px, и на 1280px**.

⚠️ Тестовых аккаунтов я не создавал — база владельца не засоряется. Проверки 1–3 требуют реального входа.

---

## Последний коммит

```
535338b  Sifaris artiq e-poct hesabi ile de verilir (ADDIM 2 tamamlandi)
```

---

## Нельзя сломать

- **Проверка входа — в одной точке:** начало `addToCart` и `toggleFavorite` в `ShopContext`. Вызывающая сторона обязана уважать возвращаемое `false`.
- **Гостевая корзина отключена намеренно** — решение переигрывалось дважды, не менять без владельца.
- **`place_order`** берёт цену на сервере. Не трогать.
- **Варианты цвета:** галерея по **папке фото**, не по коду (коды у цветов общие).
- Фото товаров на localhost дают 404 — это норма, смотреть на живом сайте.
