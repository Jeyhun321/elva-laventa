# HANDOFF — текущий статус

Обновлён: **2026-08-01 02:25**

## Где остановились

Шаг 2 (заказ и по email, не только через Google) — по коду был закончен ещё в `535338b`, но **не работал**: блокировка сидела не во фронте, а в базе.

- `src/lib/orders.js` и `src/pages/CheckoutPage.jsx` — уже на «любой вошедший» (`isSignedIn` / `AUTH_REQUIRED`), `isGoogleUser` в них не осталось. Правок не потребовалось.
- **Настоящая причина отказа:** SQL-функция `place_order` требует `provider = 'google'` и кидает `GOOGLE_AUTH_REQUIRED`. Есть во всех трёх версиях функции. RLS-политики на Google не завязаны — только эта функция.
- Создан **`supabase/fix-order-any-auth.sql`** — копия самой свежей версии (`fix-telegram-order-notify.sql`, с Telegram) минус блок проверки Google. Всё остальное не тронуто.

Незакоммиченная работа Codex (`ShopContext.jsx`, `ProductPage.jsx`) — на месте, сборка проходит.

## Что делать дальше — владельцу

1. **Применить SQL.** Supabase → SQL Editor → New query → вставить `supabase/fix-order-any-auth.sql` целиком → Run. Без этого email-покупатель заказ не оформит.
2. **Выключить подтверждение email** (только через панель, кодом нельзя):
   - Authentication → Sign In / Providers → **Email** → выключить **Confirm email** → Save.
   - Уже зарегистрированным с неподтверждённой почтой: Authentication → Users → «…» у пользователя → **Confirm email**.
3. **Живая проверка на реальных аккаунтах:**
   - Email-покупатель: регистрация → вход → корзина → оформление заказа (заказ должен дойти и в базу, и в Telegram).
   - Google-аккаунт: корзина и избранное на месте.
   - A login → данные A → logout → пусто → B login → только B → logout → пусто → A login → снова A.

Тестовые аккаунты не создавать без разрешения владельца.

## Проверено

- **Корзины/избранное не потеряны.** Чтением из базы: `customer_cart_items` — 2 строки / 1 пользователь, `customer_favorites` — 4 / 3, `orders` — 8 заказов / 3 пользователя. Для Google `accountId` = тот же `user.id`, ключ localStorage не менялся, база — источник истины.
  - *Остаточный риск:* Codex убрал автоперенос локального кэша в базу (`mustPush`). Если у кого-то корзина жила ТОЛЬКО в браузере и не доехала в базу — она пропадёт молча. По данным базы таких случаев не видно.
- **Toggle размера** — вживую на 375px и 1280px: выбор → снятие → выбор → переключение на другой размер. Консоль и логи Vite чистые.

## Не ломать

- Supabase schema/RLS и `place_order` (серверная цена, проверка размеров, Telegram, очистка корзины).
- Привязку корзины и избранного к `user.id`.
- Гостевая корзина намеренно отключена.
- Роутинг и i18n; новые тексты — через `t()` на трёх языках.
- Галерея цветовых вариантов строится по папке изображения, не по общему коду товара.

## Git

- Ветка: `main`.
- Изменены: `src/context/ShopContext.jsx`, `src/pages/ProductPage.jsx` (правки Codex), новый `supabase/fix-order-any-auth.sql`, handoff-файлы.
