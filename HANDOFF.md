# HANDOFF — текущий статус

Обновлён: **2026-08-01 01:18**

> Сейчас владелец работает с Claude Code. Codex не меняет исходный код и ведёт только `HANDOFF.md` и сегодняшний дневник до нового поручения.

## Где остановились

- Шаг 2 (email/password в дополнение к Google) завершён в коде. Последний тематический коммит: `535338b`.
- Незакоммиченная работа Codex:
  - `src/context/ShopContext.jsx` — account-owned localStorage cache, пустой успешный ответ Supabase считается корректным, автоматический перенос cache в новый аккаунт отключён, данные скрываются до `loadedAccountId === accountId`.
  - `src/pages/ProductPage.jsx` — повторный tap/click по выбранному размеру снимает выбор через функциональное обновление state.
- Сборка после обеих правок проходит.

## Что делать дальше

Нужна живая проверка владельцем на реальных аккаунтах:

1. Email-покупатель: регистрация → вход → корзина → оформление заказа.
2. Google-аккаунт: корзина и избранное сохраняются.
3. A login → данные A → logout → пусто → B login → только B → logout → пусто → A login → снова A.
4. Toggle размера проверить на 375px и 1280px.

Тестовые аккаунты не создавать без разрешения владельца.

## Не ломать

- Supabase schema/RLS и `place_order`.
- Привязку корзины и избранного к `user.id`.
- Гостевая корзина намеренно отключена.
- Роутинг и i18n; новые тексты — через `t()` на трёх языках.
- Галерея цветовых вариантов строится по папке изображения, не по общему коду товара.

## Git

- Ветка: `main`, соответствует `origin/main` до незакоммиченных правок.
- Изменены только `src/context/ShopContext.jsx` и `src/pages/ProductPage.jsx`.
